{
  "name": "hoodrain",
  "version": "1.0.0",
  "description": "ETH Distribution Protocol for Robinhood Chain token holders",
  "scripts": {
    "compile":        "hardhat compile",
    "test":           "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network robinhoodTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network robinhood",
    "gen":            "node scripts/generate-rain.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "merkletreejs": "^0.3.11"
  }
}
